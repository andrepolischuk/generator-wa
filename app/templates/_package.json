{
  "name": "<%= name %>",
  "description": "",
  "version": "0.0.0",
  "repository": "",
  "dependencies": {
    "normalize.css": "^3.0.3"
  },
  "devDependencies": {
    "babelify": "^6.2.0",
    "browserify": "^11.0.1",
    "postcss": "^5.0.2",
    "postcss-cli": "^2.0.0",
    "postcss-import": "^7.0.0"
  },
  "scripts": {
    "prebuild": "rm -rf build && mkdir build",
    "build": "npm run build-js && npm run build-css",
    "build-js": "browserify --transform [babelify --stage 0] index.js > build/index.js",
    "build-css": "postcss --use postcss-import index.css > build/index.css"
  },
  "author": "",
  "license": "MIT"
}
